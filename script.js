let botao = document.getElementById ('botao')
let mensagem = document.getElementById ('mensagem')


botao.addEventListener ('click', function(){
   mensagem.textContent = '"Grandes conquistas começam com pequenas decisões diárias. O esforço de hoje constrói os resultados de amanhã."';
});

